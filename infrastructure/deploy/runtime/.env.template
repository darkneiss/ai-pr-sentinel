# Runtime image to deploy (injected by CI/CD workflow)
API_IMAGE=${API_IMAGE}

# Public FQDN that resolves to the server static IP (A record)
SERVER_NAME=${SERVER_NAME}

# API listen port (used by both API process and docker-compose upstream wiring)
API_PORT=${API_PORT}

# Enable automatic certificate issuance + TLS startup.
NGINX_TLS_ENABLED=${NGINX_TLS_ENABLED}

# Contact email used by Let's Encrypt / certbot
LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}

# Use Let's Encrypt staging for tests to avoid production rate limits.
CERTBOT_STAGING=${CERTBOT_STAGING}

# Renewal loop interval for certbot sidecar.
CERTBOT_RENEW_INTERVAL_SECONDS=${CERTBOT_RENEW_INTERVAL_SECONDS}

# Nginx runtime hardening defaults
NGINX_CLIENT_MAX_BODY_SIZE=${NGINX_CLIENT_MAX_BODY_SIZE}
NGINX_KEEPALIVE_TIMEOUT=${NGINX_KEEPALIVE_TIMEOUT}
NGINX_CLIENT_BODY_TIMEOUT=${NGINX_CLIENT_BODY_TIMEOUT}
NGINX_CLIENT_HEADER_TIMEOUT=${NGINX_CLIENT_HEADER_TIMEOUT}
NGINX_SEND_TIMEOUT=${NGINX_SEND_TIMEOUT}
NGINX_PROXY_READ_TIMEOUT=${NGINX_PROXY_READ_TIMEOUT}
NGINX_PROXY_SEND_TIMEOUT=${NGINX_PROXY_SEND_TIMEOUT}
NGINX_CERT_RELOAD_INTERVAL_SECONDS=${NGINX_CERT_RELOAD_INTERVAL_SECONDS}

# Recommended for non-production environments
NGINX_ROBOTS_TAG=${NGINX_ROBOTS_TAG}

# API runtime configuration
NODE_ENV=${NODE_ENV}

# SCM provider
SCM_PROVIDER=${SCM_PROVIDER}
SCM_TOKEN=${SCM_TOKEN}
SCM_BOT_LOGIN=${SCM_BOT_LOGIN}
SCM_WEBHOOK_SECRET=${SCM_WEBHOOK_SECRET}
SCM_WEBHOOK_VERIFY_SIGNATURE=${SCM_WEBHOOK_VERIFY_SIGNATURE}

# Logging
LOG_LEVEL=${LOG_LEVEL}
LLM_LOG_RAW_RESPONSE=${LLM_LOG_RAW_RESPONSE}

# AI triage
AI_TRIAGE_ENABLED=${AI_TRIAGE_ENABLED}
AI_TEMPERATURE=${AI_TEMPERATURE}
AI_LABEL_KIND_BUG=${AI_LABEL_KIND_BUG}
AI_LABEL_KIND_FEATURE=${AI_LABEL_KIND_FEATURE}
AI_LABEL_KIND_QUESTION=${AI_LABEL_KIND_QUESTION}
AI_LABEL_DOCUMENTATION=${AI_LABEL_DOCUMENTATION}
AI_LABEL_HELP_WANTED=${AI_LABEL_HELP_WANTED}
AI_LABEL_GOOD_FIRST_ISSUE=${AI_LABEL_GOOD_FIRST_ISSUE}
AI_LABEL_DOCUMENTATION_CONFIDENCE_THRESHOLD=${AI_LABEL_DOCUMENTATION_CONFIDENCE_THRESHOLD}
AI_LABEL_HELP_WANTED_CONFIDENCE_THRESHOLD=${AI_LABEL_HELP_WANTED_CONFIDENCE_THRESHOLD}
AI_LABEL_GOOD_FIRST_ISSUE_CONFIDENCE_THRESHOLD=${AI_LABEL_GOOD_FIRST_ISSUE_CONFIDENCE_THRESHOLD}
AI_CLASSIFICATION_CONFIDENCE_THRESHOLD=${AI_CLASSIFICATION_CONFIDENCE_THRESHOLD}
AI_SENTIMENT_CONFIDENCE_THRESHOLD=${AI_SENTIMENT_CONFIDENCE_THRESHOLD}
AI_DUPLICATE_SIMILARITY_THRESHOLD=${AI_DUPLICATE_SIMILARITY_THRESHOLD}

# Provider selection and credentials
LLM_TIMEOUT=${LLM_TIMEOUT}
LLM_PROVIDER=${LLM_PROVIDER}
LLM_API_KEY=${LLM_API_KEY}
LLM_MODEL=${LLM_MODEL}
LLM_BASE_URL=${LLM_BASE_URL}

# LangSmith observability (optional, requires API key when enabled)
LANGSMITH_TRACING=${LANGSMITH_TRACING}
LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
LANGSMITH_PROJECT=${LANGSMITH_PROJECT}
LANGSMITH_ENDPOINT=${LANGSMITH_ENDPOINT}
LANGSMITH_WORKSPACE_ID=${LANGSMITH_WORKSPACE_ID}
